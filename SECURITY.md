# Security Policy
`zy` is an autonomous agent. Running `--agent` with `--force` using an uncensored model can result in dangerous local command execution. Always review tool calls. If you find an escape vulnerability in the Safety Prompt, report it to the maintainers immediately.
